import 'package:flutter/material.dart';

class GeometryInputPage extends StatelessWidget {
  const GeometryInputPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
